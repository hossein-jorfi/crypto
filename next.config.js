/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://academy.finobit.io'],
    domains: ['api.coingecko.com'],
  },
}

module.exports = nextConfig
